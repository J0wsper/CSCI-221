#include <stdlib.h>
#include <stdio.h>
#include "lab3_imp.h"

int main() {
    /*queue* new_queue = create_queue();
    int arr[7] = {1,2,3,4,5,6,7};
    merge(new_queue, arr, 7);
    print_queue(new_queue);
    enqueue(new_queue, 14);
    print_queue(new_queue);
    dequeue(new_queue);
    print_queue(new_queue);
    delete_queue(new_queue);
    */
    return 0;
}